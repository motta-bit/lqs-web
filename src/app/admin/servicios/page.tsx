export default function AdminServiciosPage() {
  return (
    <div className="p-8">
      <h1 className="font-authority text-4xl tracking-wider mb-2" style={{ color: 'var(--imi-textPrimary)' }}>SERVICIOS</h1>
      <p className="font-mono-data text-xs" style={{ color: 'var(--imi-textMuted)' }}>Gestión de servicios — conecta la base de datos para activar.</p>
    </div>
  )
}
